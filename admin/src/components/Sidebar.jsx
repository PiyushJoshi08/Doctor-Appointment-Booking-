import React, { useContext } from 'react'
import { AdminContext } from '../context/AdminContext'

const Sidebar = () => {

    const {aToken}=useContext(AdminContext) //storing admin Token here

  return (
    <div>
      
    </div>
  )
}

export default Sidebar
